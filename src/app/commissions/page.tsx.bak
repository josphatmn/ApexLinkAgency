'use client';

import { Suspense } from 'react';
import CommissionsContent from './content';

export default function CommissionsPage() {
  return (
    <Suspense fallback={<div className="mx-auto max-w-7xl px-4 py-8"><div className="p-8 text-center text-sm text-zinc-500">Loading...</div></div>}>
      <CommissionsContent />
    </Suspense>
  );
}

// Rename the original file content to CommissionsContent

